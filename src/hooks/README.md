# hooks/

Reusable React hooks that bridge stores/services to components, e.g.
`useGameLoop`, `useLevel`, `useHaptics`, `useInterstitialOnLevelComplete`,
`useResponsiveGrid`. Added from **Phase 3** onward as features land.
